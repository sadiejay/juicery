import recipes from '../data/recipes';
import Juice from './Juice';

/* 
This component is rendered when the user clicks the Juices link in the nav bar. It should
  - render all juice recipes as Juice components
*/
function JuicesContainer() {
  // creates a list of Juice components that each map to a juice recipe

  return (
    <div>
      <h2>Juices</h2>
    </div>
  );
}

export default JuicesContainer;
